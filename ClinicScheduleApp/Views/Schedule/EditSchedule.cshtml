@model Schedule

@{
    ViewData["Title"] = "Edit Schedule";
}

<form asp-controller="Schedule" asp-action="ProcessEditRequest" method="post" enctype="application/x-www-form-urlencoded">
    <p>
        <partial name="_ScheduleForm"></partial>
    </p>
    @* @* partial view

    <div class="text-danger" asp-validation-summary="All"></div>

    <div class="form-group">
        <label asp-for="Name">Name</label>
        <input type="text" asp-for="Name" class="form-control" />
    </div>

    <div class="form-group">
        <label asp-for="DateCreated">Date created</label>
        <input type="datetime" asp-for="DateCreated" class="form-control" value="@Model.DateCreated?.ToString("d")" />
    </div>

    @* partial view *@

    <input type="hidden" asp-for="ScheduleId" />

    <button type="submit" class="btn btn-primary">Edit</button>
    <a asp-controller="Schedule" asp-action="GetAllSchedules" class="btn btn-primary">Cancel</a>
</form>
